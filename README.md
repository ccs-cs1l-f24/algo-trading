# algo-trading

A basic algorithmic trading workspace with visualization and some basic trading strategies.

Use `main.py` to run the simulation, and change the arguments in `trader_list` to test different strategies.
